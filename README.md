# Get That File Into Your Repo
`giti` makes super quick repo insertion quick as possible.

## Replace a File Without a Clone
You don't want to clone and then remove a directory all to do a little change. If you are do a single file replacement, `giti` makes it trivial.